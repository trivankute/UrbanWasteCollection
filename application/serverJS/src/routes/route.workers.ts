import {Express} from 'express'
import catchAsync from "../utils/catchAsync";

export default function (app: Express) {
    const baseUrl="/workers"
    // get all workers
    // app.get(baseUrl+"/all", catchAsync(getAllWorkersHandle))
    
}